# Topology Description

This lab consists of two LANs connected by two routers.

PC1 → Switch → R1 → R2 → Switch → PC2

R1 and R2 are connected via a WAN link using the 2001:DB8:A:12::/64 network.

Each router is responsible for routing traffic between its local LAN and the remote LAN.