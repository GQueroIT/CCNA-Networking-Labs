# IPv6 Routing Notes

- IPv6 routing must be enabled manually using:
  ipv6 unicast-routing

- Routers do not forward IPv6 traffic by default

- Static routes are required when multiple routers are present

- Each router must know how to reach remote networks