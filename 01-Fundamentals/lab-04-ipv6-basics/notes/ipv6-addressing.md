# IPv6 Addressing Notes

- IPv6 uses 128-bit addresses
- /64 is the standard subnet size
- Each interface must be assigned a unique network
- Link-local addresses are automatically generated
- Default gateways must be in the same subnet as the host